<Window x:Class="IndustrialAutomationStudio.Modules.Motion.Views.Dialogs.AddAxisDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加轴配置"
        Width="440"
        Height="360"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#F5F7FA">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,20">
            <TextBlock FontSize="20" FontWeight="SemiBold" Text="添加离线轴配置" />
            <TextBlock Margin="0,6,0,0" Foreground="#667085"
                       Text="用于未连接控制卡或未扫描到的轴。" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="52" />
                <RowDefinition Height="52" />
                <RowDefinition Height="52" />
                <RowDefinition Height="52" />
            </Grid.RowDefinitions>
            <TextBlock VerticalAlignment="Center" Text="卡号" />
            <TextBox Grid.Column="1" Height="36" VerticalContentAlignment="Center"
                     Text="{Binding CardNo, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Grid.Row="1" VerticalAlignment="Center" Text="轴号" />
            <TextBox Grid.Row="1" Grid.Column="1" Height="36" VerticalContentAlignment="Center"
                     Text="{Binding AxisNo, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Grid.Row="2" VerticalAlignment="Center" Text="轴名称" />
            <TextBox Grid.Row="2" Grid.Column="1" Height="36" VerticalContentAlignment="Center"
                     Text="{Binding AxisName, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Grid.Row="3" VerticalAlignment="Center" Text="轴类型" />
            <ComboBox Grid.Row="3" Grid.Column="1" Height="36" VerticalContentAlignment="Center"
                      ItemsSource="{Binding AxisTypes}"
                      SelectedItem="{Binding AxisType, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="88" Height="36" Margin="0,0,10,0" Content="取消"
                    Command="{Binding CancelCommand}" />
            <Button Width="88" Height="36" Content="添加" IsDefault="True"
                    Command="{Binding ConfirmCommand}" />
        </StackPanel>
    </Grid>
</Window>
