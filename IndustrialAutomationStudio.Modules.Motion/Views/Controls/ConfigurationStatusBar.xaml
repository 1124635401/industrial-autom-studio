<UserControl x:Class="IndustrialAutomationStudio.Modules.Motion.Views.Controls.ConfigurationStatusBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Height="40" Padding="14,0" Style="{StaticResource MotionInfoBannerStyle}"
            ToolTip="{Binding StatusMessage}">
        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Path Width="18" Height="18" Margin="0,0,12,0" Stretch="Uniform"
                      Stroke="{StaticResource MotionBrush.Primary}" StrokeThickness="1.6"
                      Data="{StaticResource MotionIcon.Info}" />
                <TextBlock VerticalAlignment="Center" FontSize="13"
                           Foreground="{StaticResource MotionTextSecondaryBrush}"
                           Text="{Binding StatusMessage}" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Margin="8,0,0,0" Padding="9,3" CornerRadius="10" Background="{StaticResource MotionDangerLightBrush}">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasValidationErrors}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock FontSize="12" Foreground="{StaticResource MotionDangerBrush}" Text="存在校验错误" />
                </Border>
                <Border Margin="8,0,0,0" Padding="9,3" CornerRadius="10" Background="{StaticResource MotionPrimaryLightBrush}">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasUnsavedChanges}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock FontSize="12" Foreground="{StaticResource MotionBrush.Primary}" Text="未保存" />
                </Border>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
