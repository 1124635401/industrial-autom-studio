<UserControl x:Class="IndustrialAutomationStudio.Modules.Motion.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Border Width="480" Padding="32" HorizontalAlignment="Center" VerticalAlignment="Center" Background="White" CornerRadius="8">
            <StackPanel>
                <TextBlock HorizontalAlignment="Center" FontSize="24" FontWeight="SemiBold" Text="{Binding Title}" />
                <TextBlock Margin="0,16,0,0" HorizontalAlignment="Center" Foreground="{StaticResource MotionBrush.Muted}" Text="{Binding Message}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
