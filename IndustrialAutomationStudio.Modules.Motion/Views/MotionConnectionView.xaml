<UserControl x:Class="IndustrialAutomationStudio.Modules.Motion.Views.MotionConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <StackPanel>
        <TextBlock Margin="0,0,0,18" FontSize="26" FontWeight="SemiBold" Text="控制卡连接" />
        <StackPanel Orientation="Horizontal" Margin="0,0,0,18">
            <Button Style="{StaticResource MotionPrimaryButton}" Content="连接" Command="{Binding ConnectCommand}" />
            <Button Style="{StaticResource MotionPrimaryButton}" Content="断开" Command="{Binding DisconnectCommand}" />
            <Button Style="{StaticResource MotionPrimaryButton}" Content="扫描轴" Command="{Binding ScanCommand}" />
        </StackPanel>
        <GroupBox Header="控制卡信息" MaxWidth="720" HorizontalAlignment="Left">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="150" /><ColumnDefinition Width="260" /></Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition /><RowDefinition /><RowDefinition /><RowDefinition /><RowDefinition /><RowDefinition /><RowDefinition />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="连接状态" /><TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding IsConnected}" />
                <TextBlock Grid.Row="1" Text="控制卡名称" /><TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding CardName}" />
                <TextBlock Grid.Row="2" Text="Driver" /><TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding DriverKey}" />
                <TextBlock Grid.Row="3" Text="固件版本" /><TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding FirmwareVersion}" />
                <TextBlock Grid.Row="4" Text="轴数量" /><TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding AxisCount}" />
                <TextBlock Grid.Row="5" Text="DI / DO" /><TextBlock Grid.Row="5" Grid.Column="1"><Run Text="{Binding DiCount}" /><Run Text=" / " /><Run Text="{Binding DoCount}" /></TextBlock>
                <TextBlock Grid.Row="6" Text="操作结果" /><TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding StatusMessage}" TextWrapping="Wrap" />
            </Grid>
        </GroupBox>
    </StackPanel>
</UserControl>
