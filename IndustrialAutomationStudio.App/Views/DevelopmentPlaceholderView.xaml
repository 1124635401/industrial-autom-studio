<UserControl x:Class="IndustrialAutomationStudio.App.Views.DevelopmentPlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="Transparent">
    <Grid>
        <Border Width="560"
                Padding="48"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Style="{StaticResource ShellCardStyle}">
            <StackPanel HorizontalAlignment="Center">
                <Border Width="72"
                        Height="72"
                        HorizontalAlignment="Center"
                        Background="{StaticResource ShellBrush.PrimaryLight}"
                        CornerRadius="18">
                    <Path Width="32"
                          Height="32"
                          Stretch="Uniform"
                          Stroke="{StaticResource ShellBrush.Primary}"
                          StrokeThickness="1.7"
                          Data="{StaticResource ShellIcon.Info}" />
                </Border>
                <TextBlock Margin="0,24,0,0"
                           HorizontalAlignment="Center"
                           FontSize="12"
                           Foreground="{StaticResource ShellBrush.AuxiliaryText}"
                           Text="{Binding ModuleTitle}" />
                <TextBlock Margin="0,8,0,0"
                           HorizontalAlignment="Center"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource ShellBrush.PrimaryText}"
                           Text="{Binding PageTitle}" />
                <Border Margin="0,18,0,0"
                        Padding="12,6"
                        HorizontalAlignment="Center"
                        Background="{StaticResource ShellBrush.WarningLight}"
                        CornerRadius="4">
                    <TextBlock FontWeight="SemiBold"
                               Foreground="{StaticResource ShellBrush.Warning}"
                               Text="{Binding StatusText}" />
                </Border>
                <TextBlock Margin="0,18,0,0"
                           MaxWidth="410"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           FontSize="13"
                           LineHeight="22"
                           Foreground="{StaticResource ShellBrush.SecondaryText}"
                           Text="{Binding Description}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
